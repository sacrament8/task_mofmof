  <h2>物件編集</h2>
  <% flash.each do |key, value| %>
    <%= content_tag(:div, value, class: "#{key}") %>
  <% end %>
  <%= render 'form' %>
  <div><%= link_to '詳細', article_path(@article) %>  |  <%= link_to '戻る', articles_path %></div>